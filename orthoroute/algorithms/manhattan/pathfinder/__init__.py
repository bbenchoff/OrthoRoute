"""PathFinder support package.

Holds the modules the live engine (unified_pathfinder.PathFinderRouter)
actually imports: config constants, the CUDA Dijkstra solver and its
persistent kernels, via kernels, and KiCad geometry helpers.
"""
